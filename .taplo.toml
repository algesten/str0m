# Taplo TOML formatter configuration
[formatting]
# Set column width to 150 before line breaking
column_width = 150
# Expand arrays that exceed column_width to multiple lines
array_auto_expand = true
# Collapse arrays to single line if they fit (but auto_expand takes precedence for long lines)
array_auto_collapse = true
